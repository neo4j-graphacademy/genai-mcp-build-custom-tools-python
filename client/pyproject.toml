[project]
name = "mcp-client"
version = "0.1.0"
description = "MCP Client for connecting to MCP servers and listing tools"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.17.0",
    "httpx>=0.27.0",
    "httpx-sse>=0.4.0",
]

